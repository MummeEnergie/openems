# Use the Docker host's hostname or IP as reachable from your browser.
# localhost only works when the browser runs on the Docker host itself.
UI_WEBSOCKET=ws://localhost:8075

# Use the same published tag for Edge and UI (e.g. 2025.10.0-7).
OPENEMS_VERSION=latest
